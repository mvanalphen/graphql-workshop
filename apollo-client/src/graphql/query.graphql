query TestQueryOperation($orderId: ID!, $customerId: ID!) {
  order(id: $orderId) {
    id
    deliveryDate
  }
  customer(id: $customerId) {
    id
    firstName
    orders {
      id
      deliveryDate
    }
  }
}
